#include <stdio.h>

int main() {
    
    int l=10,b=5;
    int area,peri;
    area=l*b;
    peri=2*(l+b);
    printf("area of rectangle = %d\n",area);
    printf("perimeter of rectangle = %d\n",peri);

}
